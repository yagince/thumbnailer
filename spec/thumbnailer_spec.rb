require 'spec_helper'

describe Thumbnailer do
  it 'has a version number' do
    expect(Thumbnailer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
